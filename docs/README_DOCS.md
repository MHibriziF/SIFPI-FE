This folder `/docs`, is used to store necessary documentation on certain practices and README files of each member's working directory (`/branch-docs`).

Every working branch should provide a documentation in the `/branch-docs` subfolders on the features/backlog that the member/owner of that branch is working on.
